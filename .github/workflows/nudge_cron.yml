name: Scheduled Habit Nudges

on:
  schedule:
    - cron: '0 7,13,19 * * *'
  workflow_dispatch: 

jobs:
  trigger_nudge:
    runs-on: ubuntu-latest
    steps:
      - name: Hit Maintenance URL
        run: |
          curl -X GET "https://staysober-56ns.onrender.com/maintenance_trigger/?key=${{ secrets.MAINTENANCE_KEY }}"